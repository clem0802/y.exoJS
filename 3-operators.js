/*---------------(==)---------------*/
The == operator converts the variable values to the same type before comparing them
also known as type coercion


/*---------------(===)---------------*/
The === strict equality operator does not use any form of type coercion, 
and to evaluate as true both the type and the value must be the same