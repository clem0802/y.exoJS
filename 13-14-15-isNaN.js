/*---------------(isNaN)---------------*/
The isNaN function determines whether a value is, or is not, a number (Not-a-Number). 
If the value is not a number, it will evaluate to true, 
and if the value is a number, it will evaluate to false.


/*---------------(undeclared VS undefined)---------------*/
Undeclared variable: a variable that isn’t declared as a variable using the var/const/let keywords.
Undefined variable: a variable that has been defined using one of the variable keywords, but hasn’t been given a value.


/*---------------(.SUBMIT())---------------*/
document.forms[0].submit() // this submits the 1st form on the page